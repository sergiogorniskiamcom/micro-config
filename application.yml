server:
  port: ${PORT:${SERVER_PORT:0}}

info:
  id: ${spring.application.name}

logging:
  level: debug

endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: false
    
cassandra:
#  contactpoints: 18.231.28.228,52.67.89.146,54.233.177.125
  contactpoints: 192.168.187.147
  port: 9042
  keyspace: teste
 # loadBalacingDc: AWS_VPC_SA_EAST_1
#  auth:
 #   user: iccassandra
 #   password: cda45c46837643c6622c9b79ec5fc410

eureka:
  client:
    serviceUrl:
      defaultZone: http://192.168.187.56:8761/eureka/

feign:
  hystrix:
    enabled: true

hystrix:
  shareSecurityContext: true
  command:
    default:
      execution:
        isolation:
          strategy: SEMAPHORE

---
spring:
  profiles: docker
  http:
    multipart:
      maxFileSize: 128MB
      maxRequestSize: 128MB

logging:
  level: warn
  logstash:
    host: logstash

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-service:8761/eureka/
